# H200 功率-吞吐测量 · 操作手册（方法学 v4）

给 **H200 服务器运维人员**：照着从上到下执行即可，无需理解实验原理。
每一步给出「命令 / 这步做什么 / 成功的样子 / 不对怎么办」。全程离线，不需要联网。

- 预计耗时：验机 ~5–8 分钟，全量测量 2–4 小时（可中断续跑）。
- 需要 `sudo` 权限，用于两件事：设置功率上限（`nvidia-smi -pl`）和**锁定 GPU 频率**
  （`nvidia-smi -lgc / -rgc`，v4 新增；两者用同一个权限，不必单独申请）。
- 最终交付：一个 `data_h200_v4/` 目录（表格 + 图 + 元数据），打包回传即可。
- 本流程已在另一台机器（V100）上端到端验证通过；脚本会自动适配本机的功率范围和频率表，
  没有任何为特定型号写死的参数。

> **和上一版（v3）的区别**：prefill 阶段从"扫功率上限"改为"锁频率扫描"——脚本会逐档把
> GPU 频率锁到指定值测量，最后自动解锁。输出目录也从 `data_h200/` 改为 `data_h200_v4/`，
> 两代数据不能混放（脚本会拒绝写入旧目录，这是有意的保护）。

---

## 你会收到两样东西

1. **代码包**（如 `powerchar.tgz`）。先解开并确保目录名是 `~/powerchar/`：
   ```bash
   mkdir -p ~/powerchar && tar xzf powerchar.tgz -C ~/powerchar --strip-components=1
   ```
   （若拿到的是现成目录，直接放到 `~/powerchar/` 即可；后续所有命令都依赖这个路径。）
2. **模型缓存**（约 44 GB，5 个模型），应已放到 `~/.cache/huggingface/hub/`。
   - 如果模型放在别的位置，先设一个环境变量指向它的上级目录：
     `export HF_HOME=/模型所在/上级目录`（该目录下应有 `hub/models--...`）。
   - 如果机器可以联网且模型没给到，也可以自动下载：
     ```bash
     cd ~/powerchar/pt_cap_gpu1/portfolio
     python3 download_models.py
     ```

进入工作目录（后续所有命令都在这里执行）：
```bash
cd ~/powerchar/pt_cap_gpu1/portfolio
```

---

## 第 1 步：确认 GPU 正常

```bash
nvidia-smi
```
- **做什么**：确认能看到 H200、驱动正常、卡当前空闲。
- **成功的样子**：列出 `NVIDIA H200`，`Memory-Usage` 接近 0，无其他人的进程占用。
- **不对怎么办**：
  - 命令报 `Driver/library version mismatch` → 驱动刚更新过没重启，**重启机器**后再试。
  - 卡上有别的进程在跑 → 等它跑完，或换一张空闲卡（记住卡号，后面 `--gpu N` 用）。
    测量期间这张卡会被反复改功率上限、锁频率，**务必独占使用**。

---

## 第 2 步：确认 Python 依赖

```bash
python3 -c "import torch, transformers, pynvml, numpy, matplotlib; \
print('torch', torch.__version__, '| cuda', torch.cuda.is_available(), \
'| transformers', transformers.__version__)"
```
- **做什么**：确认测量需要的库都在。
- **成功的样子**：打印出版本，且 `cuda True`，`transformers` 版本 ≥ 4.51。
- **不对怎么办**：缺哪个装哪个（离线机器需提前备好 wheel 包）：
  ```bash
  pip install -r ~/powerchar/requirements.txt
  ```
  - `cuda False` → 装的是 CPU 版 torch，需换成对应 CUDA 的 GPU 版。
  - `transformers` < 4.51 → 升级它（其中一个模型 Qwen3 需要 ≥4.51）。
  - 只缺 `matplotlib` → 不影响测量，只影响最后自动出图；装不上也可以跑（见第 6 步说明）。

---

## 第 3 步：打开持久模式（推荐）

```bash
sudo nvidia-smi -pm 1
```
- **做什么**：让 GPU 驱动常驻，功率/频率设置更稳定、能耗计数更准。
- **成功的样子**：提示 `Persistence mode ... Enabled`。
- **不对怎么办**：报权限错就确认当前账号能 `sudo`；不是必需步骤，跳过也能跑，但数据质量略降。

---

## 第 4 步：验机（务必先做，约 5–8 分钟）

```bash
./run_all.sh --gpu 0 --smoke
```
> 如果测量卡不是 0 号，把 `--gpu 0` 换成实际卡号。
> 如果当前账号 `sudo` 需要密码，改成：
> `SUDO_PASS='这里填密码' ./run_all.sh --gpu 0 --smoke`

- **做什么**：只跑一个小负载（4 个锁频点 + 4 个功率点），把「自检 → 测量 → 出图」整条
  链路走一遍，验证这台机器能正常测。自检会顺带做一次**良性**的锁频测试（锁到全量程再
  立即解锁，对机器无影响）。
- **成功的样子**：中间出现 `preflight: READY`；每个测量点打印一行吞吐/功率，形如
  ```
  === CLK 1020 MHz (cap 700 W) ===
    prefill b=8 S=512   |    12345 tok/s |   420W | sm 1020/1020 | 29.4 tok/J
  === CAP 313 W ===
    decode  b=64 C=256   |      958 tok/s |   308W | sm 540 | 39 steps/2.6s | spread 0.1% (2 runs)
  ```
  屏幕最后出现
  ```
  == DONE. Everything to bring back is inside: data_h200_v4_smoke/ ...
  final power limit: 700.00 W        (或本机开跑前的原有上限，说明设置已自动复位)
  ```
  注意验机结果**特意**写在独立的 `data_h200_v4_smoke/` 目录（4 个点的粗数据，不是交付物，
  验完可删），不会影响后面全量测量。
- **不对怎么办**：脚本会在第一时间停下并打印原因。对照最后一节「常见问题」。
  最常见是**权限**（见下）或**某个模型没拷全**。

---

## 第 5 步：正式全量测量

验机通过后，跑完整实验：
```bash
./run_all.sh --gpu 0
```
> **重要**：只要这台机器的 `sudo` 平时需要输密码，全量运行就**必须**带上
> `SUDO_PASS='密码' ./run_all.sh --gpu 0`——即使刚才验机没带也通过了。
> （原因：sudo 会把你在第 3 步输入的密码缓存约 15 分钟，短的验机能蹭到缓存，
> 几小时的全量运行中途缓存失效后每个测量点都会失败。）

- **做什么**：对 10 个负载各测两条曲线——prefill 阶段**锁频扫描**（默认 12 档频率，
  从最低档到本机最高频），decode 阶段**扫功率上限**（默认 10 档，取本机 [最低, 最高]
  约束范围），并自动拟合出图。
- **耗时**：约 2–4 小时。屏幕持续滚动每个负载、每个测量点的结果。
- **这些是正常现象，不是故障**：
  - 某些 prefill 点行尾出现 `[power-limited]`——频率锁到高档时卡达到功率墙、
    实际频率到不了设定值，这正是要测的现象；
  - 出现 `[stop] effective clock saturated ...`——说明再往上锁也没有变化，
    脚本自动跳过剩余频率档，进入下一阶段（所以部分负载的 prefill 行数少于 12，正常）；
  - decode 高功率档的实测功率明显低于设定值——负载天然功耗不到上限，正常。
- **可中断**：中途断了（断连、误关）**没关系**——重新执行同一条命令即可，已测完的负载
  会自动跳过、从断点接着测。异常退出时脚本会自动解锁频率、恢复功率上限（含被 kill 的情形）。
- **成功的样子**：最后同样出现 `== DONE ... data_h200_v4/ ==` 和 `final power limit` 复位行。

> 想让它在后台跑、断开 SSH 也不停：
> ```bash
> nohup ./run_all.sh --gpu 0 > run.out 2>&1 &        # sudo 要密码就在 nohup 后加 SUDO_PASS='...'
> tail -f run.out        # 随时看进度，Ctrl-C 只退出查看、不影响后台任务
> ```
> 想**中途停止**后台任务：`pkill -TERM -f run_portfolio.py`（测量进程会自己解锁频率、
> 恢复功率上限后退出）。**不要用 `kill -9`**——那会跳过自动复位；万一用了，按第 7 步手动复位。

---

## 第 6 步：确认结果并打包回传

```bash
ls -la data_h200_v4/
```
> 目录名以屏幕上 `== DONE. ... inside: <目录>/ ==` 打印的为准（由 GPU 名自动生成；
> 若机器是 H200 NVL，会是 `data_h200-nvl_v4/`——把下面命令里的目录名照着替换即可）。

- **成功的样子**：目录里应有
  - `meta.json`（这台机器的 GPU 信息、频率表、两套扫描网格与测量参数）
  - `*_prefill.csv` 和 `*_decode.csv` 共约 20 个（每个负载两份；prefill 每行是一个
    锁频点，含 `clk_set_mhz` 设定频率和 `power_limited` 标志两列）
  - 四张图：`fig_decode_models.png`、`fig_prefill_models.png`、
    `fig_portfolio_grid.png`、`fig_tmax_validation.png`
  - 两张拟合表：`decode_model_compare.csv`、`portfolio_fits.csv`（及 `prefill_model_compare.csv`）
  - `run_all.log`（完整日志）
  - 若本机没装 matplotlib：图和拟合表会缺失，**属预期**——CSV + meta.json + log 照常打包
    回传即可，图我们拿回数据自己画。

打包回传：
```bash
tar czf data_h200_v4.tgz data_h200_v4/
```
把 `data_h200_v4.tgz` 传回即可，**这就是全部要交付的东西**。

---

## 第 7 步：离场检查（收尾）

```bash
nvidia-smi -i 0 -q -d POWER | grep "Power Limit"                  # 卡号按实际替换
nvidia-smi -i 0 --query-gpu=clocks.sm --format=csv,noheader       # 空闲时应是低频（几百 MHz 以下）
```
- **做什么**：确认功率上限已恢复、频率锁已解除（脚本正常结束和绝大多数异常退出都会
  自动复位；只有极端情况——比如 `kill -9`、整机断电——才需要手动处理）。
- **如果没复位**（`Current Power Limit` 明显偏低，或空闲时 `clocks.sm` 卡在某个高频值不动）：
  ```bash
  sudo nvidia-smi -i 0 -rgc                    # 解除频率锁（卡号按实际替换）
  sudo nvidia-smi -i 0 -pl <恢复值>             # 恢复功率上限
  ```
  恢复值优先用 `data_h200_v4/meta.json` 里 `pl_enforced_at_start_w` 的数值（开跑前的原有
  上限）；拿不到时再用 `Default Power Limit` 那一行的数值。

---

## 常见问题速查

| 屏幕上看到 | 含义 | 处理 |
|---|---|---|
| `[FAIL] nvidia-smi -pl privilege` | 没有设置功率上限的权限 | 用 `sudo ./run_all.sh ...` 跑，或命令前加 `SUDO_PASS='密码'`，或让管理员给 `nvidia-smi` 加免密 sudo |
| `[FAIL] nvidia-smi -lgc/-rgc privilege ...` | 没有锁频权限（v4 的 prefill 需要） | 同上——和 `-pl` 是同一个权限；若确认驱动不支持锁频，联系我们，可先 `./run_all.sh --gpu 0 --phase decode` 只测 decode |
| `[FAIL] <某模型>` | 该模型没拷全 | 重新从来源机把对应 `models--...` 整个目录（含 `snapshots/`、`blobs/`）拷到 `~/.cache/huggingface/hub/` |
| `[FAIL] transformers` | 版本太旧 | `pip install 'transformers>=4.51'` |
| `[FAIL] torch.cuda` | torch 用不了 GPU | 装 CUDA 版 torch（与本机 CUDA 匹配的 GPU 版） |
| `Driver/library version mismatch` | 驱动更新未重启 | 重启机器 |
| `ERROR: ... holds pre-v4 data` | 输出目录指到了旧版数据 | 不要用 `--outdir` 指旧目录；用默认的 `data_h200_v4/` 或另起新目录 |
| `-pl ... FAILED` / `-lgc ... FAILED`（测量中途某点） | 个别设置点失败 | 一般可忽略，脚本会跳过该点继续；若大量出现，检查 sudo 权限是否中途失效 |
| `[power-limited]` / `[stop] effective clock saturated` | 到达功率墙（预期现象） | 不用处理，这正是要测的数据 |
| `matplotlib missing -- skipped plots` | 没装 matplotlib，只影响出图 | 可忽略（CSV 照常回传），或 `pip install matplotlib` 后重跑同一条命令（测量自动跳过，只补图） |
| 中途断了 | — | 重跑 `./run_all.sh --gpu 0`，自动续跑 |
| 想彻底重测 | — | `FORCE=1 ./run_all.sh --gpu 0` |

---

## 一页速查（贴在手边）

```bash
cd ~/powerchar/pt_cap_gpu1/portfolio     # 进目录
nvidia-smi                               # 1. 看卡是否正常空闲
sudo nvidia-smi -pm 1                    # 2. 开持久模式
./run_all.sh --gpu 0 --smoke             # 3. 验机（~5-8 分钟）
./run_all.sh --gpu 0                     # 4. 全量测量（~2-4 小时，可断点续跑）
ls data_h200_v4/ && tar czf data_h200_v4.tgz data_h200_v4/   # 5. 打包回传
nvidia-smi -q -d POWER | grep "Power Limit"                  # 6. 离场检查
```
> 账号 sudo 需要密码时，把 `./run_all.sh` 换成 `SUDO_PASS='密码' ./run_all.sh`。
> 不是 0 号卡时，把 `--gpu 0` 换成实际卡号。

有问题请把 `data_h200_v4/run_all.log` 一并回传，据此即可定位。
