# H200 功率-吞吐测量 · 操作手册

给 **H200 服务器运维人员**：照着从上到下执行即可，无需理解实验原理。
每一步给出「命令 / 这步做什么 / 成功的样子 / 不对怎么办」。全程离线，不需要联网。

- 预计耗时：验机 ~5 分钟，全量测量 2–4 小时（可中断续跑）。
- 需要 `sudo` 权限（用于设置 GPU 功率上限 `nvidia-smi -pl`）。
- 最终交付：一个 `data_h200/` 目录（表格 + 图 + 元数据），打包回传即可。

---

## 你会收到两样东西

1. **代码目录**（假设放在 `~/powerchar/`）。
2. **模型缓存**（约 44 GB，5 个模型），应已放到 `~/.cache/huggingface/hub/`。
   - 如果模型放在别的位置，先设一个环境变量指向它的上级目录：
     `export HF_HOME=/模型所在/上级目录`（该目录下应有 `hub/models--...`）。
如果没有，就

```bash
cd ~/powerchar/pt_cap_gpu1/portfolio
```
```bash
python3 download_models.py     # 自动把这 5 个模型下到 H200 本地缓存
```


进入工作目录（后续所有命令都在这里执行）：
```bash
cd ~/powerchar/pt_cap_gpu1/portfolio
```

---

## 第 1 步：确认 GPU 正常

```bash
nvidia-smi
```
- **做什么**：确认能看到 H200、驱动正常、卡当前空闲。
- **成功的样子**：列出 `NVIDIA H200`，`Memory-Usage` 接近 0，无其他人的进程占用。
- **不对怎么办**：
  - 命令报 `Driver/library version mismatch` → 驱动刚更新过没重启，**重启机器**后再试。
  - 卡上有别的进程在跑 → 等它跑完，或换一张空闲卡（记住卡号，后面 `--gpu N` 用）。

---

## 第 2 步：确认 Python 依赖

```bash
python3 -c "import torch, transformers, pynvml, numpy; \
print('torch', torch.__version__, '| cuda', torch.cuda.is_available(), \
'| transformers', transformers.__version__)"
```
- **做什么**：确认测量需要的库都在。
- **成功的样子**：打印出版本，且 `cuda True`，`transformers` 版本 ≥ 4.51。
- **不对怎么办**：缺哪个装哪个（离线机器需提前备好 wheel 包）：
  ```bash
  pip install -r ~/powerchar/requirements.txt
  ```
  - `cuda False` → 装的是 CPU 版 torch，需换成对应 CUDA 的 GPU 版。
  - `transformers` < 4.51 → 升级它（其中一个模型 Qwen3 需要 ≥4.51）。

---

## 第 3 步：打开持久模式（推荐）

```bash
sudo nvidia-smi -pm 1
```
- **做什么**：让 GPU 驱动常驻，功率/时钟设置更稳定、能耗计数更准。
- **成功的样子**：提示 `Persistence mode ... Enabled`。
- **不对怎么办**：报权限错就确认当前账号能 `sudo`；不是必需步骤，跳过也能跑，但数据质量略降。

---

## 第 4 步：验机（务必先做，约 5 分钟）

```bash
./run_all.sh --gpu 0 --smoke
```
> 如果测量卡不是 0 号，把 `--gpu 0` 换成实际卡号。
> 如果当前账号 `sudo` 需要密码，改成：
> `SUDO_PASS='这里填密码' ./run_all.sh --gpu 0 --smoke`

- **做什么**：只跑一个小负载、4 个功率点，把「自检 → 测量 → 出图」整条链路走一遍，验证这台机器能正常测。
- **成功的样子**：屏幕最后出现
  ```
  == DONE. Everything to bring back is inside: data_h200/ ...
  final power limit: 700.00 W        (或本机默认值，说明功率上限已自动复位)
  ```
  中间会看到 `preflight: READY`，以及每个功率点打印一行吞吐/功率。
- **不对怎么办**：脚本会在第一时间停下并打印原因。对照最后一节「常见问题」。
  最常见是**权限**（见下）或**某个模型没拷全**。

---

## 第 5 步：正式全量测量

验机通过后，跑完整实验：
```bash
./run_all.sh --gpu 0
```
（同样，需要密码就前面加 `SUDO_PASS='...'`。）

- **做什么**：对 10 个负载各自扫功率上限，测 prefill 和 decode 两个阶段的功率-吞吐曲线，并拟合出图。
- **耗时**：约 2–4 小时。屏幕持续滚动每个负载、每个功率点的结果。
- **可中断**：中途断了（断连、误关）**没关系**——重新执行同一条命令即可，已测完的负载会自动跳过、从断点接着测。
- **成功的样子**：最后同样出现 `== DONE ... data_h200/ ==` 和 `final power limit` 复位行。

> 想让它在后台跑、断开 SSH 也不停：
> ```bash
> nohup ./run_all.sh --gpu 0 > run.out 2>&1 &
> tail -f run.out        # 随时看进度，Ctrl-C 只退出查看、不影响后台任务
> ```

---

## 第 6 步：确认结果并打包回传

```bash
ls -la data_h200/
```
- **成功的样子**：目录里应有
  - `meta.json`（这台机器的 GPU 信息与测量参数）
  - `*_prefill.csv` 和 `*_decode.csv` 共约 20 个（每个负载两份）
  - `fig_decode_models.png`、`fig_portfolio_grid.png`、`fig_tmax_validation.png` 三张图
  - `run_all.log`（完整日志）

打包回传：
```bash
tar czf data_h200.tgz data_h200/
```
把 `data_h200.tgz` 传回即可，**这就是全部要交付的东西**。

---

## 第 7 步：离场检查（收尾）

```bash
nvidia-smi -q -d POWER | grep "Power Limit"
```
- **做什么**：确认功率上限已恢复正常（脚本正常结束会自动复位；异常中断时手动查一下）。
- **如果没复位**（`Current Power Limit` 明显偏低）：手动恢复默认：
  ```bash
  sudo nvidia-smi -i 0 -pl <Default Power Limit 那一行的数值>
  ```

---

## 常见问题速查

| 屏幕上看到 | 含义 | 处理 |
|---|---|---|
| `nvidia-smi -pl privilege ... FAIL` | 没有设置功率上限的权限 | 用 `sudo ./run_all.sh ...` 跑，或命令前加 `SUDO_PASS='密码'`，或让管理员给 `nvidia-smi` 加免密 sudo |
| `[FAIL] <某模型>` | 该模型没拷全 | 重新从来源机把对应 `models--...` 整个目录（含 `snapshots/`、`blobs/`）拷到 `~/.cache/huggingface/hub/` |
| `[FAIL] transformers` | 版本太旧 | `pip install 'transformers>=4.51'` |
| `[FAIL] torch.cuda` | torch 用不了 GPU | 装 CUDA 版 torch（与本机 CUDA 匹配的 GPU 版） |
| `Driver/library version mismatch` | 驱动更新未重启 | 重启机器 |
| `-pl ... FAILED`（测量中途某点） | 个别功率点设置失败 | 一般可忽略，脚本会跳过继续；若大量出现，检查 sudo 权限是否中途失效 |
| 中途断了 | — | 重跑 `./run_all.sh --gpu 0`，自动续跑 |
| 想彻底重测 | — | `FORCE=1 ./run_all.sh --gpu 0` |

---

## 一页速查（贴在手边）

```bash
cd ~/powerchar/pt_cap_gpu1/portfolio     # 进目录
nvidia-smi                               # 1. 看卡是否正常空闲
sudo nvidia-smi -pm 1                    # 2. 开持久模式
./run_all.sh --gpu 0 --smoke             # 3. 验机（~5 分钟）
./run_all.sh --gpu 0                     # 4. 全量测量（~2-4 小时，可断点续跑）
ls data_h200/ && tar czf data_h200.tgz data_h200/   # 5. 打包回传
```
> 账号 sudo 需要密码时，把 `./run_all.sh` 换成 `SUDO_PASS='密码' ./run_all.sh`。
> 不是 0 号卡时，把 `--gpu 0` 换成实际卡号。

有问题请把 `data_h200/run_all.log` 一并回传，据此即可定位。
